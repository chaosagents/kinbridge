---
name: Bug report
about: Something in the bridge isn't working right
title: "[Bug] "
labels: bug
---

> **⚠️ Before you paste anything: redact your logs and screenshots.**
> `api_debug.log`, files under `session_logs/`, and the dashboard feed
> itself all contain your actual conversations with Ani and the Kindroid
> cast. Nobody needs to see your canon to help debug this — black out
> character names, dialogue, and anything else you'd rather keep private.
> API keys should never appear in logs, but double-check before posting.

**What happened**

A clear description of the bug.

**What you expected**

What you thought would happen instead.

**Steps to reproduce**

1.
2.
3.

**Environment**

- OS (Windows/macOS/Linux):
- Python version (`python3 --version`):
- Kinbridge version (top-right pill in the dashboard, e.g. `v28`):
- Which API(s) involved (xAI / Kindroid / Anthropic / Gemini / OpenAI):

**Logs (redacted!)**

Paste the relevant lines from `api_debug.log` or the console window here,
with any conversation text and keys removed. `HTTP xxx` status codes and
endpoint names are usually enough — you rarely need the full message
body.

```
paste here
```
